apiVersion: v1
kind: Service
metadata:
  name: {{ include "company-local-ai.fullname" . }}
  labels:
    {{- include "company-local-ai.labels" . | nindent 4 }}
  annotations:
  {{- toYaml .Values.service.annotations | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "company-local-ai.selectorLabels" . | nindent 4 }}
