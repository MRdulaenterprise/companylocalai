apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "company-local-ai.fullname" . }}-test-connection"
  labels:
    {{- include "company-local-ai.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "company-local-ai.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
